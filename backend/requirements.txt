# --- Web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # image upload (multipart/form-data)
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1

# --- DB (async). Default is SQLite; asyncpg included for PostgreSQL ---
SQLAlchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
greenlet==3.1.1

# --- ML ---
# The .pkl models were trained on scikit-learn 1.8.0 (a nightly/dev build not yet
# on PyPI). The latest stable, 1.6.1, loads and runs them correctly — it only emits
# a benign InconsistentVersionWarning (filtered at load time). If you ever have the
# exact 1.8.0 nightly, install it to silence the warning entirely.
scikit-learn==1.6.1
joblib==1.4.2
pandas==2.2.3
numpy==2.1.3

# EfficientNet-B0 soil-image classifier (best_model.pth)
torch==2.5.1
torchvision==0.20.1
Pillow==11.0.0

# --- Dev / testing helper (scripts/mock_esp32.py) ---
httpx==0.28.1
